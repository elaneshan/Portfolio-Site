map $request_method $post_limit_key {
    POST $binary_remote_addr;
    default "";
}

limit_req_zone $post_limit_key zone=timeline_post_limit:10m rate=1r/m;

server {
    listen 80;
    server_name elaneshane.duckdns.org;
    if ($host = elaneshane.duckdns.org) {
        return 301 https://$host$request_uri;
    }
}
server {
    listen 443 ssl;
    server_name elaneshane.duckdns.org;

    location /api/timeline_post {
        limit_req zone=timeline_post_limit;
        proxy_pass http://myportfolio:5000/api/timeline_post;
    }

    location / {
        proxy_pass http://myportfolio:5000/;
    }

    ssl_certificate /etc/letsencrypt/live/myportfolio/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/myportfolio/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/myportfolio/chain.pem;
}
